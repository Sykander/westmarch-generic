# Downtime

`!downtime` shows your available downtime workdays.

Use `!downtime <amount>` to add or spend workdays. Signed numbers and dice expressions are accepted, and `!downtime spend <amount>` always spends the result.

Use `!downtime setup` to create the `wg_downtime` cvar if needed. Use `!downtime reset yes` to clear the balance.
