Performs a configured downtime job roll and applies the server's reward rules.

Usage: `!job [job name] [bonuses]`

Job names use the shared lookup behavior when the server defines multiple jobs. If no job is supplied, the command uses the server's default job when one is configured.

Use `!job help` for server-aware runtime help.

Configured under: `Economy -> job`
