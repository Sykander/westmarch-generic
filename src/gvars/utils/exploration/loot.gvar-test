monster = {"name": "Wolf", "cr": 0.25, "type_str": "Beast"}
entries = loot.build_lootables(monster, {})
return len(entries) == 3 and entries[0]["skill"] == "Investigation" and entries[1]["skill"] == "Nature"
---
True
---
name: build_lootables creates investigation and type-based entries

session = {"creature": "Wolf", "items": []}
return "Wolf" in loot.format_session(session, {}) and "No lootables" in loot.format_session(session, {})
---
True
---
name: format_session handles empty active session

session = {
    "creature": "Wolf",
    "items": [
        {"id": "survey", "name": "Survey", "skill": "Investigation", "dc": 10},
    ],
}
cfg = {"subsystems": {"exploration": {"config": {"show_check_dcs": {"loot": False}}}}}
text = loot.format_session(session, cfg)
return "Investigation check" in text and "DC" not in text
---
True
---
name: format_session hides loot dcs when configured
