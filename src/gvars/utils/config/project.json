{
  "name": "avrae-gvar-utils-config",
  "projectType": "library",
  "sourceRoot": "src/gvars/utils/config",
  "tags": ["scope:avrae", "type:gvar-utils"],
  "implicitDependencies": ["avrae-sourcemaps", "avrae-gvar-utils-core"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["avraeGvarUtilBase"],
      "outputs": [],
      "options": {
        "command": "avrae-ls --log-level INFO --run-tests src/gvars/utils/config"
      }
    }
  }
}
