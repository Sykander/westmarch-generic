# Static web output

This folder is reserved for the generated GitHub Pages site.

The planned editor build will emit `index.html` and bundled static resources here. Do not store TSV catalogues in this folder; source catalogues live in [assets/](../assets/).

Until the editor is implemented, this folder intentionally has no generated site files.
